# 🛒 WiseShopPlan

**Умный планировщик покупок еды и рецептов без пищевых отходов**

---

## 📖 Содержание
- [🎯 Цель проекта](#-цель-проекта)
- [📐 Контекстная диаграмма](#-контекстная-диаграмма)
- [🏗️ Архитектура](#️-архитектура)
- [🧱 Микросервисная архитектура](#-микросервисная-архитектура)
- [📡 Событийно-ориентированная архитектура](#-событийно-ориентированная-архитектура)
- [🚀 Инструкция к запуску](#-инструкция-к-запуску)

---

## 🎯 Цель проекта

Цель - проектирование умного планировщика рецептов и покупок, который будет учитывать сроки годности продуктов во избежание пищевых отходов.

Проект сделан как **фронтенд-приложение на HTML, CSS и JavaScript**

Проект помогает:
- ✅ Планировать рацион на неделю (включая здоровое питание)
- ✅ Подбирать рецепты из имеющихся продуктов дома
- ✅ Генерировать список покупок с учётом уже имеющихся запасов, предупреждая, что эти запасы уже имеются в определенном количестве
- ✅ Предупреждать о скором истечении срока годности
- ✅ В итоге: сокращать количество пищевых отходов и экономить бюджет

---

## 📐 Контекстная диаграмма

В решении задействованы **два ключевые роли** и **четыре логические подсистемы**, обеспечивающие сквозной цикл от ввода продуктов до получения рецепта и меню.

### 🔹 Роли:
- **Пользователь** — конечный пользователь: вводит продукты, их сроки годности, запрашивает рецепты и просматривает меню.  
- **Сервис YandexGPT** — внешняя ИИ-система: генерирует рецепты и адаптирует их под запросы.  

### 🔹 Подсистемы:

1. **Ввод продуктов и сроков**  
   Эта подсистема отвечает за динамическую отрисовку интерфейса ввода и регистрацию новых продуктов с указанием срока годности. После ввода данные передаются в *Управление запасами* (инвентарь).

2. **Управление запасами**  
   Компонент отвечает за хранение продуктов, отслеживание приближения сроков годности и асинхронное обновление состояния. Все операции логируются в *Журналирование запасов*.

3. **Журналирование запасов**  
   Данный модуль фиксирует все действия с продуктами: добавление, использование, истечение срока. Данные используются для аналитики и в подсистеме *Отмена операции*.

4. **Отмена операции**  
   Этот компонент позволяет отменить добавление продукта или изменение срока даже после его подтверждения — например, при ошибке ввода. Для обеспечения целостности он работает независимо от основного потока.

---

## 🏗️ Архитектура

Решение следует принципу **«база данных на сервис»** — ключевому паттерну микросервисной архитектуры, особенно важному в событийно-ориентированных системах.

Этот шаблон предполагает, что:
- каждый микросервис владеет своей собственной базой данных (или набором данных),
- **только этот сервис имеет право читать и изменять** соответствующие данные,
- прямой доступ к данным извне запрещён — взаимодействие происходит только через API или события.

### ✅ Применение в WiseShopPlan

В текущей архитектуре выделяются **три логические подсистемы**, каждая со своей изолированной областью данных:

| Подсистема | Ответственность | Данные (в будущем) |
|------------|----------------|---------------------|
| **Ввод продуктов и сроков** | Регистрация новых продуктов, указание срока годности | `registered_items` — временные/ожидающие подтверждения продукты |
| **Управление запасами** | Хранение актуальных продуктов, учёт использования, контроль сроков | `active_inventory` — активные продукты с датами и состоянием |
| **Журналирование запасов** | Фиксация всех операций: добавление, расход, истечение срока | `inventory_log` — immutable журнал событий |

> 🔐 **Только соответствующая подсистема может работать со своей БД**. Например:
> - `Управление запасами` может изменять `active_inventory`, но **не может** писать в `inventory_log` напрямую — только через событие, которое обрабатывает `Журналирование`.
> - `Журналирование` — **единственный** сервис, обладающий правом записи в `inventory_log`.

Это обеспечивает:
- **Согласованность данных**
- **Отказоустойчивость** (сбой в одной подсистеме не нарушает другие)
- **Гибкость развёртывания и масштабирования**
- Поддержку **событийной модели** (через шины событий: Kafka, RabbitMQ или вебхуки)

> 💡 На текущем этапе данные хранятся во `localStorage` (клиентская изоляция), но логически структурированы по данному паттерну — это позволяет легко перейти к полноценной микросервисной архитектуре в будущем.

---

## 🧱 Микросервисная архитектура

Решение соответствует ключевым принципам микросервисной архитектуры:

- ✅ **Высокая доступность и тестируемость**  
  Каждый сервис может быть протестирован изолированно (unit, contract, e2e). Сбой в одном компоненте (например, журналировании) не нарушает работу планировщика или ввода продуктов.

- ✅ **Низкая связанность (loose coupling)**  
  Сервисы взаимодействуют через чётко определённые интерфейсы (API/events), а не через общую БД или прямые вызовы. Это позволяет менять реализацию одного сервиса без влияния на другие.

- ✅ **Независимо развертываемые компоненты**  
  Например, `frontend` и `api-gateway` можно обновлять отдельно; подсистема уведомлений — деплоить в отдельном окружении с иным SLA.

- ✅ **Ориентация на бизнес-возможности**  
  Каждый сервис соответствует конкретной бизнес-функции: *план питания*, *анализ сроков*, *подбор рецептов*, *формирование покупок*. Это упрощает понимание системы как бизнесом, так и разработчиками.

- ✅ **Владение малыми командами**  
  Одна небольшая команда (2 человека) может полностью владеть циклом разработки, тестирования и эксплуатации одного сервиса.

> 📌 Вывод: текущая архитектура логически соответствует микросервисному паттерну. При переходе от monolithic frontend к распределённой системе с backend-сервисами — масштабирование будет естественным и безопасным.

---

## 📡 Событийно-ориентированная архитектура

Особенность этой конструкции заключается в том, что данные могут обрабатываться несколькими сервисами асинхронно. Это ярко выражено за счёт существования отдельной сущности — **очереди событий**, в которой накапливаются операции, ожидающие выполнения.

В нашем случае за это отвечает **сервис управления запасами**, в котором, помимо своей собственной базы данных (см. *Database-per-Service*), реализована **очередь событий на основе Apache Kafka**.

Пример потока:
1. Пользователь добавляет продукт → генерируется событие `ProductRegistered`.
2. Событие публикуется в **тему Kafka `inventory.events`**.
3. **Сервис управления запасами** потребляет событие и добавляет продукт в `active_inventory`.
4. **Сервис журналирования запасов** также подписан на ту же тему и фиксирует операцию в `inventory_log`.
5. При приближении срока генерируется `ExpiryAlert`, на которое реагируют:  
   - `Журналирование` (запись),  
   - `Уведомления` (в будущем — пуши/почтa),  
   - `Аналитика` (агрегация по категориям).

> 📌 Таким образом, решение использует паттерн **событийно-ориентированной архитектуры**, где:
> - события — неизменяемые факты о бизнес-процессах,
> - обработка — децентрализована и асинхронна,
> - шина (Kafka) обеспечивает **надёжную доставку**, **масштабируемость** и **отказоустойчивость**.

Это позволяет:
- легко подключать новые реакции на события (например, интеграция с умными весами),
- избежать «спагетти» синхронных вызовов,
- гарантировать **eventual consistency** даже при сетевых сбоях.

> 💡 На текущем этапе (frontend-only) события эмулируются через `CustomEvent`, но логически схема сохранена — переход к Apache Kafka будет прямолинейным.

---

## 🚀 Инструкция к запуску

На текущем этапе проект реализован как **единый статический веб-файл** `WiseShopPlan.html`, не требующий бэкенда.

Для локального запуска выполните:

```bash
# 1. Склонируй репозиторий
git clone https://

# 2. Запустить локальный сервер для сайта (иначе API не работает (не сработает))
python -m http.server 8000
