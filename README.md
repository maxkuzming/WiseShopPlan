# 🛒 WiseShopPlan

**Умный планировщик покупок еды и рецептов без пищевых отходов**

---

## 📖 Содержание
- [🎯 Цель проекта](#-цель-проекта)
- [📐 Контекстная диаграмма](#-контекстная-диаграмма)
- [🏗️ Архитектура](#️-архитектура)
- [🧱 Микросервисная архитектура](#-микросервисная-архитектура)
- [📡 Событийно-ориентированная архитектура](#-событийно-ориентированная-архитектура)
- [❓ Вопросы по прочитанному](#-Вопросы-по-прочитанному)
- [🚀 Инструкция к запуску](#-инструкция-к-запуску)

---

## 🎯 Цель проекта

Цель - проектирование умного планировщика рецептов и покупок, который будет учитывать сроки годности продуктов во избежание пищевых отходов.

Проект проектируется как **Windows приложение на python**

Проект помогает:
- ✅ Планировать рацион на неделю, учитывая ваши продукты и предпочтения,
- ✅ Подбирать рецепты из уже имеющихся продуктов дома,
- ✅ Генерировать список покупок с учётом уже имеющихся запасов, предупреждая, что эти запасы уже имеются в определенном количестве
- ✅ Предупреждать о скором истечении срока годности
- ✅ В итоге: помогать сокращать количество пищевых отходов и экономить бюджет

---

## 📐 Контекстная диаграмма
<img width="681" height="717" alt="image" src="https://github.com/user-attachments/assets/04e53836-02fc-47f5-a1a7-656c9ca4b7ab" />


В нашем решении задачи задействованы **ключевая роль** и **несколько логических подсистемы**, обеспечивающие путь от ввода продуктов до получения рецепта, меню и планов.

### 🔹 Роли:
- **Пользователь** — конечный пользователь: вводит продукты, их сроки годности, запрашивает рецепты и просматривает меню.  

### 🔹 Подсистемы:

1. **Работа  с рецептами**  
   Эта подсистема отвечает за -.

2. **Планирование питания**  
   Компонент отвечает за -.

3. **Планирование меню на неделю**  
   Данный модуль -.
   
4. **Управление продуктами**
   Эта подсистема, которая отвечает за - .

5. **Формирование списка покупок**  
   Этот компонент позволяет -.

---

## 🏗️ Архитектура

Проект построен на двух ключевых архитектурных паттернах:
- **Database per Service** — изоляция данных по подсистемам,
- **Event-Driven Architecture** — обработка через асинхронные события.

Оба подхода обеспечивают масштабируемость, отказоустойчивость и границы ответственности.

---

## Database per Service («База данных на сервис»)

Этот паттерн — основа микросервисной архитектуры и обязательное условие для корректной работы событийной модели.

### Пояснение
Каждая подсистема владеет **собственным набором данных** и является **единственным**, кто имеет право на **чтение и запись** в них. Прямой доступ извне запрещён: взаимодействие — только через API или события.

### Применение

| Подсистема | Отвечает | Хранилище | Доступ |
|------------|----------------|-----------|--------|
| **Регистрация операций** | Приём и валидация новых запросов | `registered_items` (временные данные) | Только подсистема *Регистрация* |
| **Обработка операций** | Выполнение логики | `active_inventory` (актуальные продукты) | Только подсистема *Обработка* |
| **Журналирование** | Фиксация всех действий для аудита и аналитики | `inventory_log` (иммутабельный журнал) | Только подсистема *Журналирование* |

> Плюсы:
> - **Целостность данных** — никто не может «изменить» инвентарь извне,
> - **Отказоустойчивость** — сбой в *Журналировании* не нарушает работу *Обработки*,
> - **Гибкость** — можно заменить СУБД в одной подсистеме, не затрагивая остальные.

---

## Событийно-ориентированная архитектура (EDA)

Позволяет нескольким сервисам **асинхронно реагировать** на одно и то же событие, не создавая жёстких лагов и ошибок.

### Пояснения
- Операции не выполняются цепочкой, что приводит к не устойчевости (ошибкам).
- Вместо этого: при изменении генерируется **событие**, которое публикуется в шину (Kafka).
- Подписчики обрабатывают его **независимо и в своём темпе**.

### Применение

Пример: **«Пользователь добавил продукт»**

1. Подсистема *Регистрация* валидирует запрос → сохраняет во `registered_items` → публикует событие:
   - json (JavaScript Object Notation)

---

## 🏗️ Архитектурные решения

Проект построен на трёх фундаментальных принципах:
- **Database-per-Service** — изоляция данных по подсистемам,  
- **Event-Driven Architecture** — асинхронная обработка через шину событий,  
- **Monolith-First** — старт как единое приложение с чёткой декомпозицией, готовой к выносу сервисов.

---

## Database-per-Service

Каждая подсистема владеет **своими данными** и **единственная**, кто имеет право на запись:

| Подсистема | Данные | Доступ |
|-----------|--------|--------|
| `inventory` | `active_inventory` — актуальные продукты с датами | Только `inventory` может менять |
| `journal` | `inventory_log` — иммутабельный журнал операций | Только добавление, никаких `UPDATE`/`DELETE` |
| `recipes` | кэш запросов к YandexGPT | Только чтение из `inventory`, запись — в кэш |

**Преимущества**:  
- Целостность: никто не может «сломать» инвентарь извне,  
- Отказоустойчивость: сбой в `journal` не нарушает работу `inventory`,  
- Гибкость: можно заменить СУБД в одной подсистеме (например, SQLite → PostgreSQL), не затрагивая остальные.

--- 

## 🧱 Микросервисная архитектура

- > *«Один сервис — одна функция»*  

---

## 🔹 Логическая декомпозиция  

| Модуль | Ответственность | Границы доступа |
|--------|----------------|-----------------|
| `inventory` | Учёт продуктов, сроков, сортировка по приоритету | **Единственный**, кто пишет в `active_inventory` |
| `recipes` | Генерация рецептов через YandexGPT | Работает **только через API**: `search_by_name()`, `search_by_ingredients()` |
| `journal` | Ведение **иммутабельного** журнала операций | Только `INSERT`, никаких `UPDATE`/`DELETE` |
| `notifications` | Напоминания о скором истечении срока | Подписывается на события из `inventory`, **не обращается к БД напрямую** |
| `ui` | Отображение (сейчас — Tkinter) | Полностью отделён от логики — вызывает функции модулей |

---

## 🔹 Ключевые принципы

- **Слабая связанность**  
  Модули общаются только через **публичные интерфейсы** (функции → HTTP/events). Прямые вызовы и общая БД запрещены.

- **Сильное зацепление**  
  `recipes` не знает, как хранится инвентарь. `notifications` не знает, как генерируются рецепты.

- **Независимая развёртка**  
  Можно обновлять `notifications`, не перезапуская `inventory`.

---

## 🔹 Путь от монолита к микросервисам  

| Этап | Что делаем | Технологии |
|------|-------------|------------|
| **0 (сейчас)** | Монолит: всё в `src/` | Python, Tkinter |
| **1** | Выносим `notifications` | `asyncio.Queue`, отдельный процесс |
| **2** | `recipes` → FastAPI-сервис | FastAPI, `/search POST`, JSON |
| **3** | `inventory` + `journal` → `data-service` | PostgreSQL, Apache Kafka, идемпотентная обработка событий |

---

## 📡 Событийно-ориентированная архитектура

WiseShopPlan использует **событийно-ориентированную архитектуру (EDA)** для обеспечения:
- **Низкой связанности** между подсистемами,
- **Высокой отказоустойчивости** (сбой одного сервиса не ломает остальные),
- **Гибкости расширения** (новые реакции на события — без изменения ядра).


## Почему не синхронные вызовы?

При синхронном сценарии:  
`UI → Inventory → Journal → Notifications`  
— одна ошибка (например, `Journal` упал) **останавливает весь процесс**. Пользователь видит «ошибка», продукт не добавлен.

В EDA:  
`UI → Inventory → публикует событие → Journal и Notifications обрабатывают независимо`  
— даже если `Journal` недоступен — продукт будет в инвентаре, а событие **дождётся восстановления**.

---

## Технологический стек проекта

| Компонент | Технология | Обоснование выбора | Преимущества |
|-----------|------------|---------------------|-------------|
|Язык программирования|Python 3+|Простота, читаемость, богатая стандартная библиотека и поддержка ИИ-интеграций|Быстрый старт MVP, лёгкость обучения, обширное сообщество, простая интеграция с REST API|
|GUI Framework|Tkinter|строен в стандартную поставку Python, кроссплатформенность (Windows/macOS/Linux)|Отсутствие внешних зависимостей, минимальный размер дистрибутива, достаточен для MVP|
|ИИ-интеграция|YandexGPT API|Поддержка русского языка, низкий порог входа, generous free tier для стартапов|Точность генерации рецептов на русском, документированное REST API, совместимость с event-driven архитектурой|
|Хранение данных|nventory.json + json модуль|Простота реализации, человекочитаемость, переносимость|Не требует СУБД, легко резервируется и синхронизируется, подходит для 1–10 тыс. записей|
|Архитектура|Модульная (core / services / ui), «монолит-first»|Чёткое разделение ответственности при минимальной сложности|Лёгкий переход к микросервисам (например, вынос recipes в FastAPI), простота тестирования, масштабируемость без переписывания|
|Событийная модель|asyncio.Queue (MVP) → Kafka (future)|Естественная поддержка асинхронности в Python, лёгкость прототипирования|Отказоустойчивость, низкая связанность, возможность горизонтального масштабирования под нагрузку|

Для хранения данных используется JSON-сериализация через встроенный модуль json. Это обеспечивает простоту развёртывания и читаемый формат без необходимости установки СУБД. Особое внимание было уделено выбору Tkinter как GUI-фреймворка в силу его лёгкости, кроссплатформенности и встроенности в стандартную библиотеку Python. Комбинация Tkinter, asyncio.Queue для событийной модели и YandexGPT API образует сбалансированный стек, оптимальный для MVP умного планировщика питания, направленного на сокращение пищевых отходов и персонализацию рациона.

---


## ❓ Вопросы по прочитанному


---

## 🚀 Инструкция к запуску
1. установить PyCharm
2. скачать и распаковать zip файл - WiseShopPlan-main.zip
3. в PyCharm нажать на File, потом на Open, выбираешь весь распакованный файл
4. если нет библиотеки requests, то установить её с официального сайта: (https://pypi.org/project/requests/)
5. пройти по всем файлам, когда установил библиотеку, и в терминале каждого файла, который оканчивается на .py прописать команду: `pip install requests`
6. нажать на main правой кнопкой мыши и нажать Run 
-
