# 🛒 WiseShopPlan

**Умный планировщик покупок еды и рецептов без пищевых отходов**

---

## 📖 Содержание
- [🎯 Цель проекта](#-цель-проекта)
- [📐 Контекстная диаграмма](#-контекстная-диаграмма)
- [🏗️ Архитектура](#️-архитектура)
- [🧱 Микросервисная архитектура](#-микросервисная-архитектура)
- [📡 Событийно-ориентированная архитектура](#-событийно-ориентированная-архитектура)
- [❓ Вопросы по прочитанному](#-Вопросы-по-прочитанному)
- [🚀 Инструкция к запуску](#-инструкция-к-запуску)

---

## 🎯 Цель проекта

Цель - проектирование умного планировщика рецептов и покупок, который будет учитывать сроки годности продуктов во избежание пищевых отходов.

Проект сделан как **фронтенд-приложение на HTML, CSS и JavaScript**

Проект помогает:
- ✅ Планировать рацион на неделю (включая здоровое питание)
- ✅ Подбирать рецепты из уже имеющихся продуктов дома
- ✅ Генерировать список покупок с учётом уже имеющихся запасов, предупреждая, что эти запасы уже имеются в определенном количестве
- ✅ Предупреждать о скором истечении срока годности
- ✅ Сокращать количество пищевых отходов (меньше загрязняет природу)
- ✅ В итоге: помогать сокращать количество пищевых отходов и экономить бюджет

---

## 📐 Контекстная диаграмма

В нашем решении задачи задействованы **две ключевые роли** и **пять логические подсистемы**, обеспечивающие цикл от ввода продуктов до получения рецепта и меню.

### 🔹 Роли:
- **Пользователь** — конечный пользователь: вводит продукты, их сроки годности, запрашивает рецепты и просматривает меню.  
- **Сервис YandexGPT** — внешняя ИИ-система: генерирует рецепты, адаптирует их под запросы, следит за истекающими продуктами, напоминает.  

### 🔹 Подсистемы:

1. **Ввод продуктов и сроков**  
   Эта подсистема отвечает за динамическую отрисовку интерфейса ввода и регистрацию новых продуктов с указанием срока годности. После ввода данные передаются в *Управление запасами*.

2. **Управление запасами**  
   Компонент отвечает за хранение продуктов, отслеживание приближения сроков годности и асинхронное обновление состояния. Все операции логируются в *Журналирование запасов*.

3. **Журналирование запасов**  
   Данный модуль фиксирует все действия с продуктами: добавление, использование, истечение срока. Данные используются для аналитики и в подсистеме *Отмена операции*.
   
4. **Уведомления**
   Эта подсистема, которая отвечает за осведомление пользователей о скором истечение срока определенного продукта.

5. **Отмена операции**  
   Этот компонент позволяет отменить добавление продукта или изменение срока даже после его подтверждения — например, при ошибке ввода. Для обеспечения целостности он работает независимо от основного потока.

---

## 🏗️ Архитектура

WiseShopPlan построен по принципу **«одна задача — один модуль»**, с чётким разделением:

- Только модуль `inventory` может изменять список продуктов,  
- Только `journal` ведёт историю операций,  
- Только `notifications` отправляет напоминания.

---

## 🏗️ Архитектурные решения






---

## 🧱 Микросервисная архитектура

| Сервис | Технологии |

---

## 📡 Событийно-ориентированная архитектура

Особенность этой конструкции заключается в том, что данные могут обрабатываться несколькими сервисами асинхронно. Это ярко выражено за счёт существования отдельной сущности — **очереди событий**, в которой накапливаются операции, ожидающие выполнения.

В нашем случае за это отвечает **сервис управления запасами**, в котором, помимо своей собственной базы данных (см. *Database-per-Service*), реализована **очередь событий на основе Apache Kafka**.

Пример потока:
1. Пользователь добавляет продукт → генерируется событие `ProductRegistered`.
2. Событие публикуется в **тему Kafka `inventory.events`**.
3. **Сервис управления запасами** потребляет событие и добавляет продукт в `active_inventory`.
4. **Сервис документирования запасов** также подписан на ту же тему и фиксирует операцию в `inventory_log`.
5. При приближении срока генерируется `ExpiryAlert`, на которое реагируют:  
   - `Документирование` (запись),  
   - `Уведомления` (в будущем — пуши/почтa),  
   - `Аналитика` (группировка по категориям).

> 📌 Таким образом, решение использует паттерн **событийно-ориентированной архитектуры**, где:
> - события — неизменяемые факты о процессах,
> - обработка — децентрализована,
> - шина (Kafka) обеспечивает **надёжную доставку**, **масштабируемость** и **отказоустойчивость**.

Это позволяет:
- легко подключать новые реакции на события (например, интеграция с умными весами),
- избежать «спагетти» синхронных вызовов,
- гарантировать **eventual consistency** даже при сетевых сбоях.

> 💡 На текущем этапе (frontend-only) события эмулируются через `CustomEvent`, но логически схема сохранена — переход к Apache Kafka будет прямолинейным.

---


## ❓ Вопросы по прочитанному

- Kubernetes — платформа для оркестрации контейнеров в кластере,
- Apache Kafka — распределённая платформа для обработки потоковых данных, часто называется брокером сообщений,
- Backend-сервис — это часть веб-приложения или программного обеспечения, которая отвечает за обработку данных, бизнес-логику и взаимодействие с базами данных, внешними API и другими системами,
- API события — это значимые события или изменения состояния в системе, которые запускают действия или изменения состояний,
- API (Application Programming Interface) — программный интерфейс приложения,
- Frontend — это область, которая фокусируется на создании пользовательского интерфейса и взаимодействии с пользователем,
- LocalStorage — это объект, встроенный в веб-браузеры, который позволяет веб-сайтам хранить информацию на компьютере пользователя,
- ConfigMap — это ресурс Kubernetes, предназначенный для хранения неконфиденциальных данных конфигурации в виде пар «ключ-значение»,
- Java — язык програмирования,
- Quarkus — это фреймворк для разработки приложений на Java, оптимизированный для работы в контейнерах и безсерверной архитектуре,
- Оркестрация - автоматизированное управление и координация различных компонентов, служб или процессов для обеспечения бесперебойной работы сложных систем,
- Full-stack реализация — это создание полного приложения, включающего как frontend (клиентская часть, с которой взаимодействует пользователь), так и backend (серверная часть, включая базу данных и логику). Такой подход позволяет разработчику самостоятельно управлять всем проектом от начала до конца, работая с пользовательским интерфейсом, бизнес-логикой и инфраструктурой,
- Облачная нативная система (Cloud Native) — это подход к разработке и развертыванию приложений, который использует преимущества облачных вычислений, такие как эластичность, масштабируемость и отказоустойчивость,
- SLA (Service Level Agreement, или соглашение об уровне обслуживания) в IT — это формализованный документ, который устанавливает договорённости между поставщиком услуг и клиентом. В нём фиксируются измеряемые показатели качества, такие как время реакции на инцидент, время восстановления работы сервиса, или процент доступности услуги, а также ответственность сторон за их невыполнение,
- Шина событий (event bus) — это паттерн проектирования, выступающий в роли централизованного канала для обмена сообщениями между компонентами системы, используя шаблон «Издатель/Подписчик» (Pub/Sub). Один компонент (издатель) отправляет событие в шину, а другие компоненты (подписчики), заинтересованные в этом событии, получают уведомление и обрабатывают его, при этом они не знают друг о друге. Это снижает связанность между частями системы, упрощая ее разработку и масштабирование. 

---

## 🚀 Инструкция к запуску

На текущем этапе проект реализован как **единый статический веб-файл** `WiseShopPlan.html`, не требующий бэкенда.

Для локального запуска выполните:

```bash
# 1. Склонируй репозиторий
git clone https://

# 2. Запустить локальный сервер для сайта (иначе API не работает (не сработает))
python -m http.server 8000
